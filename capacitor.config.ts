import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pablohigueras.bitacorafit',
  appName: 'bitacorafit',
  webDir: 'www',
};

export default config;
